name: Send Random String on Push

on:
  workflow_dispatch: # 手动触发 (变量引用的时候使用 ${{ github.event.inputs.tag_name }})
    inputs:
      tag_name:
        description: 'Tag name for the release'
        required: true
        default: 'v1.0.0'
jobs:
  send-email:
    runs-on: ubuntu-latest
    env:
      EMAIL_USERNAME: ${{ secrets.EMAIL_USERNAME }}
      EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Use environment variables
        run: |
          echo "Using EMAIL_USERNAME: ${{ env.EMAIL_USERNAME }}"
          echo "Using EMAIL_PASSWORD: ${{ env.EMAIL_PASSWORD }}"

      - name: Send email
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.163.com
          server_port: 465
          username: ${{ env.EMAIL_USERNAME }}
          password: ${{ env.EMAIL_PASSWORD }}
          subject: "new Tag on Push"
          body: "A push event occurred. New tag is ${{ github.event.inputs.tag_name }}"
          to: 414078812@qq.com
          from: ${{ env.EMAIL_USERNAME }}
          content_type: text/plain